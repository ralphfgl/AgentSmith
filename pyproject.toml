[project]
name = "agentsmith"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ralph", email = "ralph8@live.fr" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp>=2.2.0",
]

[project.scripts]
agentsmith = "agentsmith:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
